# 執筆設計書

## 執筆アプローチ

コードファーストの方針に従い、各節で先にコード例・実行結果を提示してから概念を解説する。3ページという制約があるため、各節を簡潔にまとめる。

## 節ごとの設計

### 1.1 Pythonのインストール

**導入**: Pythonを使うためにまず実行環境を準備する。

**図表設計**:
- 表1.1: OS別（Windows / macOS / Linux）のインストール方法を簡潔にまとめた表

**本文の構成**:
1. Python公式サイトからのダウンロード
2. OS別インストール手順（表で簡潔に）
3. `python --version`でバージョン確認

### 1.2 対話モードとスクリプト実行

**導入**: インストールが完了したPythonには二つの実行方法がある。

**図表設計**:
- 図1.1: Mermaidフローチャートで「試行錯誤？→対話モード / まとまった処理？→スクリプト」の判断フロー

**本文の構成**:
1. 対話モードの起動（`python`コマンド）と簡単な計算
2. スクリプトファイルの作成と実行（`python hello.py`）
3. 使い分けの基準

### 1.3 最初のプログラム

**導入**: 実行方法を理解したところで、定番のHello, World!を書く。

**図表設計**:
- 図1.2: Mermaidフローで「コード記述→インタプリタが解釈→結果出力」の流れ

**本文の構成**:
1. `print("Hello, World!")`の実行
2. 複数行のprint文
3. プログラムが上から順に実行されることの確認

## 章の導入

本書の目標（Pythonの基礎を20ページで習得）を簡潔に示し、まず環境構築から始める旨を伝える。

## 章の結び

`print()`で固定の文字列を出力できるようになった。次章では、値を変数に格納して柔軟に操作する方法を学ぶ。

## 理解度チェック問題の設計

1. 対話モードとスクリプト実行の違いと適切な使い分け（概念確認）
2. 場面に応じた実行方法の選択（判断問題）
3. `print()`関数の役割の説明（概念確認）
