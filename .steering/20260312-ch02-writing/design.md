# 執筆設計書

## 執筆アプローチ

コードファーストの方針に従い、各節で先にコード例を提示してから概念を解説する。4ページの中で変数・データ型・演算子の3トピックをバランスよくカバーする。

## 節ごとの設計

### 2.1 変数と代入

**導入**: 第1章では固定の文字列を`print()`で出力した。値を変数に格納することで柔軟な処理が可能になる。

**図表設計**:
- 図2.1: Mermaid graphで変数名→オブジェクトの参照関係を図示

**本文の構成**:
1. 変数への代入と`print()`での出力
2. 図2.1による参照モデルの解説
3. 再代入と動的型付けの説明
4. 変数名の命名規則（スネークケース）

### 2.2 基本データ型

**導入**: 変数に格納できる値にはさまざまな種類がある。

**図表設計**:
- 表2.1: int/float/str/boolの一覧表（型名、説明、例）

**本文の構成**:
1. `type()`関数で型を確認するコード例
2. 表2.1で4つのデータ型を整理
3. 型変換（int(), str(), float()）の使用例

### 2.3 演算子

**導入**: データ型を学んだところで、値に対する演算を行う方法を学ぶ。

**図表設計**:
- 表2.2: 演算子の種類（算術・比較・論理）と動作一覧

**本文の構成**:
1. 算術演算子のコード例
2. 比較演算子のコード例
3. 論理演算子のコード例
4. 文字列の結合と繰り返し

## 章の導入

第1章で`print()`を使い固定の文字列を出力した。値を変数に格納して操作する方法を学ぶ。

## 章の結び

変数に値を格納し演算できるようになった。比較演算子やブール値を使い、次章で条件分岐を学ぶ。

## 理解度チェック問題の設計

1. 動的型付けとは何か（概念確認）
2. 与えられた式の実行結果を予測（判断問題）
3. `//`と`/`の違い（概念確認）
